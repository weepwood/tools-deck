{
  "name": "tools-deck",
  "private": true,
  "version": "0.4.0",
  "description": "A native desktop workbench for local tools and scripts",
  "type": "module",
  "scripts": {
    "dev": "vite --port 1420",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "desktop:icons": "tauri icon src-tauri/app-icon.svg",
    "desktop:dev": "npm run desktop:icons && tauri dev",
    "desktop:build": "npm run desktop:icons && tauri build",
    "desktop:test": "npm run desktop:icons && cargo test --manifest-path src-tauri/Cargo.toml",
    "desktop:check": "npm run desktop:icons && cargo check --manifest-path src-tauri/Cargo.toml",
    "check": "node --check vite.config.js && node --check src/data/tools.js && node --check src/domain/manifest.js && node --check src/hooks/useLocalStorage.js && node --check src/runtime/runtime.js && node --check src/runtime/nativePaths.js && node --check src/runtime/artifactActions.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "@tauri-apps/plugin-dialog": "2.7.2",
    "@tauri-apps/plugin-opener": "2.5.4",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "@vitejs/plugin-react": "6.0.4",
    "vite": "8.1.5"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
