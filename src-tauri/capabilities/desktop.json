{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Tools Deck 主窗口桌面权限",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:allow-open-path",
    "opener:allow-reveal-item-in-dir"
  ]
}
